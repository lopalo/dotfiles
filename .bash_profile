alias r='reset'

alias gr='grep -rni'

alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

set -o vi

PATH=$PATH:~/bin

[ -f /usr/local/etc/bash_completion ] && . /usr/local/etc/bash_completion

export LC_ALL=en_US.UTF-8

